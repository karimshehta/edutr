// Modern spacing scale (8px base unit)
export const spacing = {
  xxs: 2,   // 2px  - micro spacing
  xs: 4,    // 4px  - tight spacing
  sm: 8,    // 8px  - small spacing
  md: 12,   // 12px - medium spacing
  lg: 16,   // 16px - large spacing  
  xl: 20,   // 20px - extra large
  xxl: 24,  // 24px - double XL
  xxxl: 32, // 32px - triple XL
  huge: 40, // 40px - huge spacing
  massive: 48, // 48px - massive spacing
} as const;

// Modern border radius (following Material Design 3 & iOS standards)
export const borderRadius = {
  none: 0,     // no radius
  xs: 4,       // 4px  - subtle
  sm: 8,       // 8px  - small (buttons, inputs)
  md: 12,      // 12px - medium (cards)
  lg: 16,      // 16px - large (panels)
  xl: 20,      // 20px - extra large (hero cards)
  xxl: 24,     // 24px - double XL (modals)
  full: 9999,  // fully rounded (avatars, pills)
} as const;

// Elevation/Shadow system (for depth perception)
export const elevation = {
  none: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 0 },
    shadowOpacity: 0,
    shadowRadius: 0,
    elevation: 0,
  },
  sm: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 1 },
    shadowOpacity: 0.05,
    shadowRadius: 2,
    elevation: 1,
  },
  md: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.08,
    shadowRadius: 4,
    elevation: 2,
  },
  lg: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.1,
    shadowRadius: 8,
    elevation: 4,
  },
  xl: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.12,
    shadowRadius: 16,
    elevation: 8,
  },
} as const;
